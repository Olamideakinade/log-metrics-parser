[package]
name = "log-metrics-parser"
version = "1.4.0"
edition = "2021"
authors = ["Core Maintainer"]

[dependencies]
clap = { version = "4.4", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
indicatif = "0.17"
console = "0.15"

[dev-dependencies]
assert_cmd = "2.0"
predicates = "3.0"
